/*
* This file is auto-generated.
 * Do not edit this file manually.
 * Your changes will be overwritten during the next generation.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Contains details about the error if the request failed.
 */
export type ErrorT = {
  /**
   * The HTTP status code that explains the type of error (for example, 400 for a bad request, 404 for not found).
   */
  code?: number | undefined;
  /**
   * A short message describing what went wrong.
   */
  message?: string | undefined;
  /**
   * A detailed explanation of the error and what caused it. May also include links to documentation or tips for fixing the issue.
   *
   * @remarks
   */
  description?: string | undefined;
};

export type DefaultError = {
  /**
   * Shows if the request was completed successfully. Returns `true` for success and `false` for failure.
   */
  success?: boolean | undefined;
  /**
   * Contains details about the error if the request failed.
   */
  error?: ErrorT | undefined;
};

/** @internal */
export const ErrorT$inboundSchema: z.ZodMiniType<ErrorT, unknown> = z.object({
  code: types.optional(types.number()),
  message: types.optional(types.string()),
  description: types.optional(types.string()),
});

export function errorFromJSON(
  jsonString: string,
): SafeParseResult<ErrorT, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ErrorT$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ErrorT' from JSON`,
  );
}

/** @internal */
export const DefaultError$inboundSchema: z.ZodMiniType<DefaultError, unknown> =
  z.object({
    success: types.optional(types.boolean()),
    error: types.optional(z.lazy(() => ErrorT$inboundSchema)),
  });

export function defaultErrorFromJSON(
  jsonString: string,
): SafeParseResult<DefaultError, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DefaultError$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DefaultError' from JSON`,
  );
}
