/*
* This file is auto-generated.
 * Do not edit this file manually.
 * Your changes will be overwritten during the next generation.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Represents an AI response record containing status and data for AI-generated features like summary, chapters, named entities, or moderation.
 */
export type AiResponseRecord = {
  /**
   * The status of the AI processing (for example, "available", "preparing", "failed").
   */
  status?: string | null | undefined;
  /**
   * The AI-generated content data. Can be a Map, List, or other structured data depending on the AI feature type.
   */
  data?: { [k: string]: any } | null | undefined;
};

/** @internal */
export const AiResponseRecord$inboundSchema: z.ZodMiniType<
  AiResponseRecord,
  unknown
> = z.object({
  status: z.optional(z.nullable(types.string())),
  data: z.optional(z.nullable(z.record(z.string(), z.any()))),
});

export function aiResponseRecordFromJSON(
  jsonString: string,
): SafeParseResult<AiResponseRecord, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AiResponseRecord$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AiResponseRecord' from JSON`,
  );
}
