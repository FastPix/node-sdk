/*
* This file is auto-generated.
 * Do not edit this file manually.
 * Your changes will be overwritten during the next generation.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Pagination organizes content into pages for better readability and navigation.
 */
export type SigningKeysPagination = {
  /**
   * It gives the total number of Signing keys that are created by a user.
   */
  totalRecords?: number | undefined;
  /**
   * Offset determines the current point for data retrieval within a paginated list.
   */
  currentOffset?: number | undefined;
  /**
   * The offset count is expressed as total records by limit
   */
  offsetCount?: number | undefined;
};

/** @internal */
export const SigningKeysPagination$inboundSchema: z.ZodMiniType<
  SigningKeysPagination,
  unknown
> = z.object({
  totalRecords: types.optional(types.number()),
  currentOffset: types.optional(types.number()),
  offsetCount: types.optional(types.number()),
});

export function signingKeysPaginationFromJSON(
  jsonString: string,
): SafeParseResult<SigningKeysPagination, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SigningKeysPagination$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SigningKeysPagination' from JSON`,
  );
}
