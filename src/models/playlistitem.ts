/*
* This file is auto-generated.
 * Do not edit this file manually.
 * Your changes will be overwritten during the next generation.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * type of the playlist, when it was created
 */
export const PlaylistItemType = {
  Manual: "manual",
  Smart: "smart",
} as const;
/**
 * type of the playlist, when it was created
 */
export type PlaylistItemType = OpenEnum<typeof PlaylistItemType>;

export type PlaylistItem = {
  /**
   * The unique id of the playlist
   */
  id?: string | undefined;
  /**
   * The name of the playlist set by the user
   */
  name?: string | undefined;
  /**
   * type of the playlist, when it was created
   */
  type?: PlaylistItemType | undefined;
  /**
   * Unique string value assigned by user to the playlist.
   */
  referenceId?: string | undefined;
  /**
   * Timestamp of playlist creation.
   */
  createdAt?: Date | undefined;
  /**
   * No. of media present in the playlist
   */
  mediaCount?: number | undefined;
};

/** @internal */
export const PlaylistItemType$inboundSchema: z.ZodMiniType<
  PlaylistItemType,
  unknown
> = openEnums.inboundSchema(PlaylistItemType);

/** @internal */
export const PlaylistItem$inboundSchema: z.ZodMiniType<PlaylistItem, unknown> =
  z.object({
    id: types.optional(types.string()),
    name: types.optional(types.string()),
    type: types.optional(PlaylistItemType$inboundSchema),
    referenceId: types.optional(types.string()),
    createdAt: types.optional(types.date()),
    mediaCount: types.optional(types.number()),
  });

export function playlistItemFromJSON(
  jsonString: string,
): SafeParseResult<PlaylistItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PlaylistItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PlaylistItem' from JSON`,
  );
}
