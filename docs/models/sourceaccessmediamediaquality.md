# SourceAccessMediaMediaQuality

The quality tier applied to the media.

## Example Usage

```typescript
import { SourceAccessMediaMediaQuality } from "@fastpix/fastpix-node/models";

let value: SourceAccessMediaMediaQuality = "standard";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"standard" | "pro" | "premium" | Unrecognized<string>
```