# DirectUploadResponseMediaQuality

The quality tier applied to the media.

## Example Usage

```typescript
import { DirectUploadResponseMediaQuality } from "@fastpix/fastpix-node/models";

let value: DirectUploadResponseMediaQuality = "standard";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"standard" | "pro" | "premium" | Unrecognized<string>
```